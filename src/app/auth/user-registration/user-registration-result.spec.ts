import { UserRegistrationResult } from './user-registration-result';

describe('UserRegistrationResult', () => {
  it('should create an instance', () => {
    expect(new UserRegistrationResult()).toBeTruthy();
  });
});
